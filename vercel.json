{
	"version": 2,
	"builds": [
		{
			"src": "src/server.ts",
			"use": "@vercel/node",
			"config": {
				"includeFiles": [
					"node_modules/swagger-ui-dist/**",
					"prisma/**"
				]
			}
		}
	],
	"routes": [
		{
			"src": "/swagger-ui/(.*)",
			"dest": "src/server.ts"
		},
		{
			"src": "/api/(.*)",
			"dest": "src/server.ts"
		},
		{
			"src": "/(.*)",
			"dest": "src/server.ts"
		}
	]
}
